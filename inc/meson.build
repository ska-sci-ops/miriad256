# Configure header files with compile-time variables
maxdim_hdr  = configure_file(input : 'maxdim.h.in',  output : 'maxdim.h',  configuration : conf_data)
maxdimc_hdr = configure_file(input : 'maxdimc.h.in', output : 'maxdimc.h',  configuration : conf_data)

inc = include_directories('.')