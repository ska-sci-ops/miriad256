# Configure header files with compile-time variables
configure_file(input : 'maxdim.h.in',  output : 'maxdim.h',  configuration : conf_data)
configure_file(input : 'maxdimc.h.in', output : 'maxdimc.h',  configuration : conf_data)
inc = include_directories('.')